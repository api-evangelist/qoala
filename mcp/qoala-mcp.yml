generated: '2026-08-05'
method: searched
source: https://docs.qoala.app/mcp
status: published
server:
  name: Qoala for Enterprise
  version: '1.3'
  transport: http
  url: https://docs.qoala.app/mcp
  protocol_version: '2025-06-18'
  auth: none (anonymous tools/list and tools/call succeeded)
  note: Documentation-plane MCP server hosted by Qoala on its ReadMe-powered developer portal. It exposes
    the 15 published Qoala OpenAPI specifications to agents for discovery and request execution; it is
    not a business-capability server (there is no create_policy / create_claim tool). Calling the Qoala
    business API through execute-request still requires a partner x-api-key.
x-evidence:
  fetched: '2026-08-05'
  url: https://docs.qoala.app/mcp
  http_status: 200
  content_type: text/event-stream
  initialize_server_info:
    name: Qoala for Enterprise
    version: '1.3'
  tools_list_count: 6
specs_exposed:
- Bus Insurance
- Claim API
- Credit Life Insurance
- Experience & Event Insurance
- Credit Insurance
- Flight Insurance
- Gadget Insurance
- Goods Insurance
- Micro health insurance
- Hotel Insurance
- Logistic Insurance
- Train Insurance
- Vehicle Insurance
- Authentication API
- Qoala Claim API
tools:
- name: list-endpoints
  description: Lists all API paths and their HTTP methods with summaries, organized by path. Results can
    be passed directly into 'get-endpoint'.
  input_schema:
    required:
    - title
    properties:
      title:
        type: string
        description: Title of the OpenAPI spec. Use tool 'list-specs' or 'search-endpoints' to see available
          specs.
- name: get-endpoint
  description: Gets detailed information about a specific API endpoint, including security schemes and
    servers
  input_schema:
    required:
    - path
    - method
    - title
    properties:
      path:
        type: string
        description: The API endpoint path (e.g. /api/v1/users).
      method:
        type: string
        description: The HTTP method (e.g. GET, POST, PUT, DELETE).
      title:
        type: string
        description: Title of the OpenAPI spec. Use tool 'list-specs' or 'search-endpoints' to see available
          specs.
- name: search-endpoints
  description: Performs a deep search through paths, operations, and parameters to discover relevant API
    endpoints. Use this tool to find specific API capabilities, required parameters, or data models based
    on search keywords. Results can be passed directly into 'get-endpoint'.
  input_schema:
    required:
    - pattern
    properties:
      pattern:
        type: string
        description: Search pattern (case-insensitive)
- name: list-specs
  description: Lists all available OpenAPI specs. Use the title to select a spec.
  input_schema:
    required: []
    properties: {}
- name: execute-request
  description: Executes an API request with a given HAR request object.
  input_schema:
    required:
    - harRequest
    - title
    properties:
      harRequest:
        type: object
        description: HAR request object
      title:
        type: string
        description: Title of the OpenAPI spec. Use tool 'list-specs' or 'search-endpoints' to see available
          specs.
- name: get-server-variables
  description: Gets the server variables for each server within a specific OpenAPI spec.
  input_schema:
    required:
    - title
    properties:
      title:
        type: string
        description: Title of the OpenAPI spec. Use tool 'list-specs' or 'search-endpoints' to see available
          specs.
deployment:
  mode: remote
  verified: searched
  tools: 6
  checked: '2026-08-12'
  source: catalog MCP census
